name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    # TODO: Update to macos-15 or later when Xcode 26 is available on GitHub runners
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        # TODO: Update to Xcode 26 when available
        run: sudo xcode-select -s /Applications/Xcode_16.3.app

      - name: Build
        run: swift build

      - name: Test
        run: swift test
